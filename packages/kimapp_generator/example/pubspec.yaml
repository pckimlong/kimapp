name: example
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  json_annotation:
  flutter:
    sdk: flutter
  kimapp:
    git: https://github.com/pckimlong/kimapp.git
    

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: 
  json_serializable: 
  kimapp_generator:
    path: ..

flutter:
  uses-material-design: true
